import { ObjcarconversionPipe } from './objcarconversion.pipe';

describe('ObjcarconversionPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjcarconversionPipe();
    expect(pipe).toBeTruthy();
  });
});
